# Interfaces Layer

Contiene las interfaces de entrada/salida al sistema.

Ejemplos:

- Controladores HTTP/REST/GraphQL.
- CLI (comandos).
- Websockets.

📌 Importante:

- Puede usar `application` para invocar casos de uso.
- Nunca debe contener lógica de negocio.
